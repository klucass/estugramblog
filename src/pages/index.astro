---
import Layout from "../layouts/Layout.astro";
import Article from "../components/Article.astro";
const allPosts = await Astro.glob("../pages/posts/*.md");
---

<Layout title="Estugram Blog">
  {allPosts.map((post) => <Article post={post} />)}
</Layout>
